<h2 class="page-header">Recently Addded</h2>
<div>
    <% books.forEach(book => { %>
    <a href="/books/<%= book.id%>">
        <img height="150" width="100" src="<%= book.coverImagePath %>">
    </a>
    <% }) %>
</div>